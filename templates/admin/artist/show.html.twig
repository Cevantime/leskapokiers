{% extends 'admin/base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ artist.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ artist.lastname }}</td>
            </tr>
            <tr>
                <th>Bio</th>
                <td>{{ artist.bio }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ artist.age }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ artist.gender }}</td>
            </tr>
            <tr>
                <th>Pseudonym</th>
                <td>{{ artist.pseudonym }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_artist_index') }}">back to list</a>

    <a href="{{ path('admin_artist_edit', {'id': artist.id}) }}">edit</a>

    {{ include('admin/artist/_delete_form.html.twig') }}
{% endblock %}
